# v0.0.5

- **Fix** — the jagged contour lines in the ocean are gone: the distance
  fields behind plate boundaries and the coastal shelf are now blurred into
  smooth gradients instead of stepped (integer) bands.
