[workspace]
resolver = "2"
members = ["game", "launcher"]

# Dev-build tuning: keep our crates unoptimised for fast iteration while the
# (large) dependency graphs compile and run optimised.
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
